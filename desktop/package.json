{
  "name": "surveyapp-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Electron shell for the Survey App. Spawns the FastAPI backend as a sidecar.",
  "author": "Survey App",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently -k -n be,fe,el -c blue,green,magenta \"npm:dev:backend\" \"npm:dev:frontend\" \"npm:dev:electron\"",
    "dev:backend": "cross-env PYTHONUNBUFFERED=1 ../backend/.venv/Scripts/python.exe -m uvicorn app.main:app --app-dir ../backend --host 127.0.0.1 --port 8765 --reload",
    "dev:frontend": "npm --prefix ../frontend run dev",
    "dev:electron": "wait-on -t 60000 http-get://localhost:5173 http-get://127.0.0.1:8765/docs && cross-env SURVEYAPP_DEV=1 VITE_DEV_URL=http://localhost:5173 electron .",
    "start": "electron .",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "wait-on": "^8.0.1"
  }
}
